# 索引系统

为三层知识分别建立索引，供 Agent 快速路由检索目标。

## 目录结构

```
indexes/
├── repos/          # 第三类：外部 Git 仓库的内容索引
├── wiki/           # 第二类：Wiki 抓取缓存的内容索引（延后至 v0.2，当前量少无需索引）
└── knowledge/      # 第一类：人工维护知识的内容索引（待建）
```

## 索引格式

每个索引文件为 Markdown，记录对应知识源的关键主题、文件路径和检索关键词。不记录全文，只记录"去哪里找什么"。
